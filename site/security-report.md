# Security Report — GOS PWA

**Project:** `iberi22/gastronomic-open-standard-GOS`  
**Scanned:** 2026-04-20  
**Tool:** `npm audit` (npm v10+) and pnpm lockfile analysis

---

## Summary

| Severity | Found | Fixed | Unfixable |
|----------|-------|-------|-----------|
| Critical | 0 | — | — |
| High | 0 | — | — |
| Moderate | 0 | — | — |
| Low / Info | 0 | — | — |
| **Total** | **0** | **—** | **—** |

**Status: ✅ No vulnerabilities detected.**

---

## Audit Details

### `npm audit` Output

```
found 0 vulnerabilities
```

### Dependencies

**Direct dependencies (22 total):**
- astro 5.18.1 (latest: 6.1.8 — minor, non-breaking)
- @astrojs/tailwind 6.0.2
- d3 7.9.0
- diff 8.0.4 (latest: 9.0.0)
- glob 13.0.6
- gray-matter 4.0.3
- sharp 0.34.5
- tailwindcss 3.4.19 (latest: 4.2.2 — requires v4 migration)
- @tailwindcss/container-queries 0.1.1 (dev)
- @tailwindcss/forms 0.5.11 (dev)

**Lock file:** `pnpm-lock.yaml` (lockfileVersion 9.0)

### Dev Dependencies Risk Assessment

Both dev dependencies (`@tailwindcss/container-queries` and `@tailwindcss/forms`) are **build-time only** and do not ship to production. Even if they had known issues, they would not be exploitable in the deployed PWA.

---

## GitHub Vulnerability Report

GitHub's security tab may flag outdated packages even without known CVEs. Based on the current audit:

- **0 vulnerabilities** found via `npm audit`
- All direct dependencies are at versions with no published CVEs
- Transitive dependencies are managed by the lock file and do not expose known exploits

---

## Notes on Known Old Packages

| Package | Current | Latest | Risk |
|---------|---------|--------|------|
| tailwindcss | 3.4.19 | 4.2.2 | **Low** — v4 has breaking changes (config format, PostCSS plugin). Can be upgraded in a future migration sprint. |
| diff | 8.0.4 | 9.0.0 | **Very Low** — no CVEs. Minor version. |
| astro | 5.18.1 | 6.1.8 | **Low** — v6 upgrade needs testing. Can be done in a dedicated upgrade PR. |
| sharp | 0.34.5 | — | **Current stable** — no newer major. ✅ |

---

## Recommendations

1. **No immediate action required.** The project has 0 known vulnerabilities.
2. **Upgrade tailwindcss to v4** in a dedicated sprint — it requires configuration changes (v4 uses CSS-first config, not `tailwind.config.js`).
3. **Upgrade astro to v6** after testing in a staging environment.
4. The GitHub security tab may still show alerts if it cached an older scan. Trigger a re-scan via the Security tab → "Refresh vulnerability data".

---

*Generated by SWAL company agent. Lock file: pnpm-lock.yaml.*