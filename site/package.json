{
  "name": "gos-site",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "verify:protocol": "node scripts/verify_protocol.js",
    "dev": "node scripts/copy-content.js && astro dev",
    "build": "npm run verify:protocol && node scripts/copy-content.js && node scripts/generate-api.js && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/tailwind": "^6.0.2",
    "astro": "^5.0.5",
    "glob": "^13.0.0",
    "gray-matter": "^4.0.3",
    "tailwindcss": "^3.4.18"
  },
  "devDependencies": {
    "@tailwindcss/container-queries": "^0.1.1",
    "@tailwindcss/forms": "^0.5.10"
  }
}
